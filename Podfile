# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'Realm-ReoderTableView-Sample' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Realm-ReoderTableView-Sample
  pod 'RealmSwift'

  target 'Realm-ReoderTableView-SampleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Realm-ReoderTableView-SampleUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
